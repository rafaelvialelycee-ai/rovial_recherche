# Pour déploiement sur Railway / Render / Heroku
# Le frontend doit être deployé séparément (Vercel / Netlify)
web: cd backend && pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port $PORT
